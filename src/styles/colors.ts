import { css } from "styled-components";

const Colors = css`
    :root {
        --black: hsl(0, 0%, 0%);
        --white: hsl(0, 0%, 100%);
        --gray: hsl(0, 0%, 63%);
        --dark-gray: hsl(0, 0%, 27%);
    }
`;

export default Colors;
